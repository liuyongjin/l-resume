# Expo assets

Run once after clone to generate default assets:

```bash
npx expo install expo-asset
# Or copy 1024x1024 PNG icons named:
# icon.png, splash-icon.png, adaptive-icon.png, favicon.png
```

Brand color for splash: `#7C3AED`

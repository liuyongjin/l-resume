server:
  port: 8088

spring:
  application:
    name: backend-admin-java
  datasource:
    url: jdbc:postgresql://localhost:5432/l_resume
    username: postgres
    password: "123456"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

jianflow:
  jwt:
    secret: "admin_jwt_secret_change_in_production"
    issuer: http://localhost:8088
    expires-in: 2h

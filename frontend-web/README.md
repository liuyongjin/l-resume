# frontend-web

简流前台 Web 应用（Nuxt 4 + Vue 3 + Tailwind + shadcn-vue）。

## 启动

```bash
npm install
npm run dev
```

默认开发地址：http://localhost:3000

## 说明

- 开发模式下 `/api/*` 代理至 `backend-nest`（默认 3001）
- 同 monorepo 内还有 `frontend-admin`（管理后台）、`frontend-mobile`（移动端）
